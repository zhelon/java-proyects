#The name of app have to be the same as a configuration
spring.application.name=bbq-master-service
#You have to define the correct url to find config-server. On this case I will use muy localhost
spring.cloud.config.uri:http://localhost:8888

#The configguration server will use the default config, to use dev config just uncomment the below code
#spring.profiles.active=dev